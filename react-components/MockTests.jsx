import React, {useState} from "react";
import ReactDOM from 'react-dom'

function GermanTest(){
   const [answers, setAnswers] = useState(['', '', '']);
   const correctAnswers = ['in der', 'Im', 'weil'];
  const [feedback, setFeedback] = useState([]);
}